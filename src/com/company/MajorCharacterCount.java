package com.company;

import java.util.Scanner;
import java.util.Arrays;

public class MajorCharacterCount {
    public static void main(String[] args) {
        //write a program to accept a string from a user and to return a character and the count of the character that
        //occurs the most in the string
      //  Scanner scanner = new Scanner(System.in);
        //System.out.println("Enter a word: ");
        //String word = scanner.nextLine();
       // int counter = 0;
       // for (int i = 0; i < word.length(); i++) {
         //   char letter = word.toLowerCase().charAt(i);
           // for (int j = 1; j < word.length(); j++) {
             //   char checkLetter =word.toLowerCase().charAt(j);
               // if letter(i) =  checkLetter(j) {
                 //   counter++
                }


}
